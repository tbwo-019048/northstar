/** Home, reached from the Dock — a bare resting screen: no cards, no
 * content, just the app shell (header, footer, dock) around a solid black
 * canvas. Not the marketing Landing page (that's "/", unauthenticated). */
export function AppHome() {
  return <div className="min-h-[70svh] rounded-md bg-black" />
}
